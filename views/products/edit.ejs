<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>Tech Gadgets & Accessories</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/products">Products</a>
      <a href="/cart">Cart</a>
      <% if (!user) { %>
        <a href="/login">Login</a>
        <a href="/register">Register</a>
      <% } else { %>
        <a href="/logout">Logout</a>
        <% if (user.isAdmin) { %>
          <a href="/admin">Admin</a>
        <% } %>
      <% } %>
    </nav>
  </header>
  <main>
    <% if (success_msg && String(success_msg).trim()) { %>
      <div class="flash-message success"><%= success_msg %></div>
    <% } %>
    <% if (error_msg && String(error_msg).trim()) { %>
      <div class="flash-message"><%= error_msg %></div>
    <% } %>
    <% if (error && String(error).trim()) { %>
      <div class="flash-message"><%= error %></div>
    <% } %>
    <h2>Edit Product</h2>
    <form action="/products/<%= product._id %>" method="POST" class="product-form">
      <input type="text" name="name" value="<%= product.name %>" required />
      <textarea name="description" required><%= product.description %></textarea>
      <input type="number" name="price" value="<%= product.price %>" step="0.01" required />
      <input type="text" name="image" value="<%= product.image %>" />
      <input type="text" name="category" value="<%= product.category %>" required />
      <input type="number" name="stock" value="<%= product.stock %>" min="0" />
      <button type="submit" class="btn">Update Product</button>
    </form>
    <form action="/products/<%= product._id %>/delete" method="POST" style="margin-top:1rem;">
      <button type="submit" class="btn btn-danger">Delete Product</button>
    </form>
    <a href="/products/<%= product._id %>" class="btn">Back to Product</a>
  </main>
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Tech Gadgets. All rights reserved.</p>
  </footer>
</body>
</html> 